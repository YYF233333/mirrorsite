name: Release

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: cargo build --verbose
    
    - name: Create release
      uses: ncipollo/release-action@v1
      with:
        artifacts: "target/release/mirrorsite"
        artifactErrorsFailBuild: true
        omitBody: true
        token: ${{ secrets.GITHUB_TOKEN }}

